TODO
====

- replace static text with locales

- don't call update status if status hasn't changed

- add another state (assumed ready) for people who don't have RRL

- when state is toggled via /rrl r, print output to console

- 